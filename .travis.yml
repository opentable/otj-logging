language: java
jdk:
  - openjdk8

before_install:
  - wget http://archive.apache.org/apache/maven/maven-3/3.6.2/binaries/apache-maven-3.6.2-bin.tar.gz
  - unzip -qq apache-maven-3.6.2-bin.zip
  - export M2_HOME=$PWD/apache-maven-3.6.2
  - export PATH=$M2_HOME/bin:$PATH

 -